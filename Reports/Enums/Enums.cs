public enum ReportGroupBy { None, Author, State, Category, Priority, Date, User, Company }
public enum ReportSortBy { Count, Name, Date }

public enum DateGroupingLevel
{
    Day,
    Week,
    Month,
    Year,
    Spring,
    Summer,
    Autumn,
    Winter
}
